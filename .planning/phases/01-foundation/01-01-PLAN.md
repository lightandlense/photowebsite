---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - .htaccess
  - css/tokens.css
  - css/base.css
  - css/grain.css
  - gallery.json
  - images/fashion/.gitkeep
  - images/beauty/.gitkeep
  - images/light-painting/.gitkeep
  - images/drone/.gitkeep
  - images/video/.gitkeep
  - images/fashion/test-sample.webp
  - images/fashion/test-sample.jpg
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-04
  - INFR-05

must_haves:
  truths:
    - "Opening index.html via a local HTTP server shows a styled page with the darkroom color palette visibly applied"
    - "The page renders the Playfair Display serif font (or Georgia fallback) with warm off-white text on a dark background"
    - "A subtle film grain texture is visible over the entire page"
    - "gallery.json loads without error in the browser console and contains 5 genre entries"
    - "The page is responsive — text and layout adapt at mobile breakpoints without horizontal overflow"
    - "A test image is served via a <picture> element with WebP source and JPEG fallback, and both files are under 200KB"
  artifacts:
    - path: "index.html"
      provides: "Single HTML entry point with GSAP CDN, Google Fonts, CSS links, script module reference, picture element with test image, and app root div"
      contains: "id=\"app\""
    - path: ".htaccess"
      provides: "Apache rewrite rules for SPA routing on cPanel"
      contains: "RewriteRule"
    - path: "css/tokens.css"
      provides: "CSS custom properties for colors, typography, timing, grain"
      contains: "--color-bg"
    - path: "css/base.css"
      provides: "CSS reset, typography, global layout rules, responsive breakpoints"
      contains: "font-family"
    - path: "css/grain.css"
      provides: "Film grain overlay via SVG feTurbulence"
      contains: "feTurbulence"
    - path: "gallery.json"
      provides: "Genre data manifest with 5 genre entries"
      contains: "genres"
    - path: "images/fashion/test-sample.webp"
      provides: "Test WebP image under 200KB for image pipeline validation"
    - path: "images/fashion/test-sample.jpg"
      provides: "Test JPEG fallback image under 200KB for image pipeline validation"
  key_links:
    - from: "index.html"
      to: "css/tokens.css"
      via: "link rel stylesheet"
      pattern: "href=\"css/tokens.css\""
    - from: "index.html"
      to: "js/main.js"
      via: "script type module"
      pattern: "src=\"js/main.js\""
    - from: "index.html"
      to: "images/fashion/test-sample.webp"
      via: "picture source element"
      pattern: "source.*test-sample\\.webp"
    - from: "css/base.css"
      to: "css/tokens.css"
      via: "CSS custom property references"
      pattern: "var\\(--"
---

<objective>
Create the HTML scaffold, CSS design system, gallery data manifest, .htaccess SPA routing, image folder structure, and a test WebP/JPEG image pair for the darkroom portfolio.

Purpose: Every subsequent phase builds on these files — the color tokens, typography, grain overlay, and HTML shell are the foundation the entire visual experience inherits. The test image proves the WebP+JPEG fallback pipeline works before any visual phase depends on it.
Output: A styled index.html served via local HTTP server showing the darkroom aesthetic (dark amber background, Playfair Display serif, film grain overlay), a test image rendered via a `<picture>` element with WebP/JPEG fallback, plus gallery.json with 5 genre placeholders and Apache rewrite rules for SPA deployment.
</objective>

<execution_context>
@C:/Users/Russell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Russell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CSS design system (tokens, base, grain)</name>
  <files>css/tokens.css, css/base.css, css/grain.css</files>
  <action>
Create three CSS files that establish the darkroom visual language.

**css/tokens.css** — CSS custom properties on `:root`. All values from the locked user decisions:
- `--color-bg: #1a1208` (deep warm black, NOT pure black)
- `--color-bg-surface: #241a0e` (slightly lighter for walls/surfaces)
- `--color-text: #e8d5b0` (warm off-white, cream/amber tint)
- `--color-accent: #c8902a` (warm gold for interactive hints — Claude's discretion per CONTEXT.md)
- `--color-amber-glow: #ff8c00` (safelight orange for glow effects)
- `--font-serif: 'Playfair Display', Georgia, serif`
- Font sizes: `--font-size-base: 1rem`, `--font-size-lg: 1.5rem`, `--font-size-xl: 2.5rem`
- Transition durations: `--duration-fast: 0.3s`, `--duration-normal: 0.8s`, `--duration-cinematic: 1.5s` (cinematic per user decision: "deliberate 1-2 second transitions")
- Grain: `--grain-opacity: 0.04`, `--grain-size: 0.65`

**css/base.css** — Global styles consuming the tokens:
- Minimal CSS reset (box-sizing border-box, margin 0, etc.)
- `body`: background `var(--color-bg)`, color `var(--color-text)`, font-family `var(--font-serif)`, font-size `var(--font-size-base)`, line-height 1.6, min-height 100vh
- `#app`: min-height 100vh, position relative
- Headings (h1-h3): use `--font-size-xl`, `--font-size-lg`, `--font-size-base` with font-weight 400 (Playfair Display is elegant at regular weight)
- Links: color `var(--color-accent)`, no underline, transition color `var(--duration-fast)`
- `::selection`: background `var(--color-accent)`, color `var(--color-bg)`
- Responsive breakpoints via media queries:
  - Mobile-first base styles (default)
  - `@media (min-width: 768px)` — tablet adjustments (font-size-base: 1.1rem)
  - `@media (min-width: 1024px)` — desktop adjustments (font-size-base: 1.125rem)
- Utility class `.visually-hidden` for accessibility (screen-reader only text)

**css/grain.css** — Film grain overlay per research Pattern 4:
- `body::before` pseudo-element with `position: fixed; inset: 0; pointer-events: none; z-index: 9999`
- opacity: `var(--grain-opacity)`
- background-image: inline SVG data URI with `feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch"`
- background-size: 200px 200px
  </action>
  <verify>
    <automated>python -m http.server 8080 --directory "E:/Antigravity/Projects/Photo Website" &amp; sleep 2 &amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/css/tokens.css &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/css/base.css &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/css/grain.css; kill %1 2>/dev/null</automated>
  </verify>
  <done>Three CSS files exist. tokens.css defines all custom properties on :root. base.css applies them globally with responsive breakpoints. grain.css renders a film grain overlay that does not block pointer events.</done>
</task>

<task type="auto">
  <name>Task 2: Create index.html, .htaccess, gallery.json, and image folders</name>
  <files>index.html, .htaccess, gallery.json, images/fashion/.gitkeep, images/beauty/.gitkeep, images/light-painting/.gitkeep, images/drone/.gitkeep, images/video/.gitkeep</files>
  <action>
**index.html** — Single HTML entry point. Follow the structure from the RESEARCH.md "Minimal index.html Structure" exactly:
- `<!DOCTYPE html>`, `<html lang="en">`
- `<head>`: charset UTF-8, viewport meta (width=device-width, initial-scale=1.0), title "Darkroom Portfolio"
- Google Fonts: preconnect hints + Playfair Display link (weights 400, 700, italic 400) with `display=swap`
- CSS links in order: tokens.css, base.css, grain.css
- `<body>`:
  - `<div id="app"></div>` — app root where scenes will be injected by later phases
  - Inside `#app`, add a temporary placeholder: `<h1>Darkroom Portfolio</h1><p>Foundation loaded.</p>` (this confirms the design system works visually; later phases will replace it)
  - GSAP CDN script tags (gsap.min.js, ScrollTrigger.min.js, ScrollSmoother.min.js) — all version 3.14.2 from jsDelivr
  - `<script type="module" src="js/main.js"></script>` — the app entry point (created in Plan 02)

IMPORTANT: Load GSAP scripts BEFORE the module script tag. Do NOT use `defer` on CDN scripts — they must be available as globals before main.js runs.

**.htaccess** — Apache SPA rewrite rules per research:
```
RewriteEngine On
RewriteBase /
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

**gallery.json** — Genre manifest per research Pattern 5. Include all 5 genres with:
- `version: 1`
- `genres` array with objects: `id`, `label`, `folder`, `clotheslineIndex` (0-4), `photoCount` (0 for now)
- Genres in order: fashion, beauty, light-painting, drone, video
- Include an empty `photos` array in each genre entry (per research open question 1 — adding this now so Phase 4 can populate it)

**Image folders** — Create empty directories with .gitkeep files:
- images/fashion/.gitkeep
- images/beauty/.gitkeep
- images/light-painting/.gitkeep
- images/drone/.gitkeep
- images/video/.gitkeep
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('E:/Antigravity/Projects/Photo Website/gallery.json')); assert len(d['genres'])==5; assert d['genres'][0]['id']=='fashion'; print('gallery.json valid')" &amp;&amp; grep -q "RewriteRule" "E:/Antigravity/Projects/Photo Website/.htaccess" &amp;&amp; echo "htaccess valid" &amp;&amp; grep -q 'id="app"' "E:/Antigravity/Projects/Photo Website/index.html" &amp;&amp; echo "index.html valid"</automated>
  </verify>
  <done>index.html loads in a browser showing "Darkroom Portfolio" in Playfair Display with the amber/dark color scheme and film grain visible. gallery.json parses with 5 genre entries. .htaccess contains SPA rewrite rules. Five genre image folders exist.</done>
</task>

<task type="auto">
  <name>Task 3: Create test WebP/JPEG image pair and add picture element to index.html</name>
  <files>images/fashion/test-sample.webp, images/fashion/test-sample.jpg, index.html</files>
  <action>
This task validates the WebP + JPEG fallback image pipeline (ROADMAP success criterion 4).

**Step 1: Generate test images using Python.**
Use Python (Pillow/PIL) to programmatically create a small test image — a 400x300 rectangle with a warm amber gradient (matching the darkroom palette). Save it as:
- `images/fashion/test-sample.webp` — WebP format, quality 80 (must be under 200KB)
- `images/fashion/test-sample.jpg` — JPEG format, quality 85 (must be under 200KB)

If Pillow is not installed, use ImageMagick CLI (`magick convert`) instead:
```bash
magick -size 400x300 gradient:"#1a1208"-"#c8902a" images/fashion/test-sample.jpg
magick images/fashion/test-sample.jpg images/fashion/test-sample.webp
```

If neither Pillow nor ImageMagick is available, create a minimal valid WebP and JPEG using Python's struct module to write the bare minimum valid file headers, or use `curl` to download a small sample from a public domain image source (e.g., picsum.photos) and convert/resize.

**Step 2: Add a `<picture>` element to index.html.**
Inside `#app`, after the existing `<h1>` and `<p>` placeholder content, add:
```html
<picture>
  <source srcset="images/fashion/test-sample.webp" type="image/webp">
  <img src="images/fashion/test-sample.jpg" alt="Test image — WebP/JPEG pipeline validation" width="400" height="300" style="margin-top: 1rem; border: 2px solid var(--color-accent); border-radius: 4px;">
</picture>
<p><small>Image pipeline test: if you see an image above, WebP/JPEG fallback is working.</small></p>
```

The `<picture>` element with `<source type="image/webp">` provides the WebP version to browsers that support it, and falls back to the `<img src>` JPEG for browsers that do not.

**Step 3: Verify file sizes are under 200KB.**
Run: `ls -la images/fashion/test-sample.*` and confirm both are under 200KB (204800 bytes).
  </action>
  <verify>
    <automated>python -c "
import os
webp = os.path.getsize('E:/Antigravity/Projects/Photo Website/images/fashion/test-sample.webp')
jpg = os.path.getsize('E:/Antigravity/Projects/Photo Website/images/fashion/test-sample.jpg')
assert webp &lt; 204800, f'WebP too large: {webp}'
assert jpg &lt; 204800, f'JPEG too large: {jpg}'
print(f'WebP: {webp} bytes, JPEG: {jpg} bytes — both under 200KB')
" &amp;&amp; grep -q "test-sample.webp" "E:/Antigravity/Projects/Photo Website/index.html" &amp;&amp; grep -q "picture" "E:/Antigravity/Projects/Photo Website/index.html" &amp;&amp; echo "picture element present in index.html"</automated>
  </verify>
  <done>Two test image files exist in images/fashion/ (test-sample.webp and test-sample.jpg), both under 200KB. index.html contains a picture element with WebP source and JPEG fallback. Serving via local HTTP server shows the test image rendered correctly on the page.</done>
</task>

</tasks>

<verification>
Start a local HTTP server and verify the full foundation renders:
1. `python -m http.server 8080` from project root
2. Open http://localhost:8080 in Chrome — page should show dark background, warm text, Playfair Display font, subtle film grain
3. Check browser console — no errors (note: js/main.js will 404 until Plan 02 runs; this is expected)
4. Verify gallery.json loads: in console run `fetch('./gallery.json').then(r=>r.json()).then(d=>console.log(d.genres.length))` — should log `5`
5. Resize to mobile width — text and layout should adapt without horizontal scroll
6. Verify the test image is visible below the placeholder text — in Chrome DevTools Network tab, confirm the WebP variant is loaded (not the JPEG); in a browser without WebP support, the JPEG fallback should load instead
</verification>

<success_criteria>
- index.html served via HTTP shows darkroom aesthetic (dark background, warm text, grain overlay)
- CSS custom properties defined and consumed correctly across all three CSS files
- gallery.json loads and parses with 5 genre entries
- .htaccess contains working Apache SPA rewrite rules
- 5 genre image folders exist in images/
- Page is responsive at mobile, tablet, and desktop breakpoints
- Test image displayed via picture element with WebP source and JPEG fallback, both files under 200KB
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
